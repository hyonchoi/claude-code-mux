# Test Plan
Generated by /plan-eng-review on 2026-05-18
Branch: main
Commit: 25ceb60 — fix: add background Copilot token refresh to prevent idle expiry
File changed: src/server/mod.rs

## Affected Code Paths

- `src/server/mod.rs` — background token refresh task spawned at server startup

## Key Interactions to Verify

1. **enterprise_url preserved after background refresh** [CRITICAL regression]
   - Set up an OAuthToken with `enterprise_url = Some("https://my-org.copilot.github.com")`
   - Run the refresh logic (extracted function) with a mock `refresh_copilot_token` that returns `Ok`
   - Assert the saved token's `enterprise_url` equals the original (not `None`)

2. **project_id preserved after background refresh**
   - Same as above but for `project_id` field

3. **Non-copilot tokens are not refreshed by background task**
   - Provider with `provider_type = "gemini"` and expiring token
   - Assert `refresh_copilot_token` is NOT called for that provider

4. **Refresh error is logged as warning, task continues**
   - Mock `refresh_copilot_token` returning `Err`
   - Assert task doesn't panic, warn log is emitted, loop continues to next provider

5. **Token not in store is silently skipped**
   - Provider config present but no matching token in store
   - Assert no panic, no crash

## Edge Cases

- Multiple Copilot providers: both should be checked independently
- Token with `needs_refresh() = false` (10+ min remaining): should NOT be refreshed
- Token already expired (past `expires_at`): should be refreshed
- `expires_at` unix timestamp overflow (u64 → i64 clamp): existing `min(i64::MAX as u64)` handles it — verify the unwrap_or fallback fires correctly for a max-u64 timestamp

## Critical Paths

- Copilot Enterprise user: idle server for >25 min → re-enable provider → first request succeeds with correct enterprise endpoint
