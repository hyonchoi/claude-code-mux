# Test Plan
Generated by /plan-eng-review on 2026-05-19
Branch: main
Repo: 9j/claude-code-mux

## Affected Code Paths

### src/server/mod.rs
- `needs_background_refresh(token, poll_interval)` — renamed/generalized from `copilot_token_needs_background_refresh`
- Background refresh dispatch loop — extended to all OAuth providers
- `provider_cooldowns: DashMap<String, Instant>` — new AppState field
- Three fallback loops: ~946 (standard), ~1198 (streaming), ~1518 (OpenAI-compat) — all three need DashMap cooldown check

### src/server/admin.html
- `escapeJs(text)` — new function, replaces `escapeHtml()` in onclick handlers
- `loadConfig()` 401 cancel path — add `if (!response.ok)` check

## Unit Tests Required (cargo test)

### needs_background_refresh (update existing 3 tests)
- `test_needs_background_refresh_near_expiry` — near-expiry returns true
- `test_needs_background_refresh_fresh_token` — fresh returns false
- `test_needs_background_refresh_already_expired` — expired returns true

### OAuth refresh dispatch (3 new tests — D8)
- `test_background_refresh_dispatch_copilot` — copilot arm calls refresh_copilot_token
- `test_background_refresh_dispatch_standard_oauth` — gemini/openai arm calls OAuthClient::refresh_token
- `test_background_refresh_api_key_only_skipped` — API-key-only providers produce no warning and no refresh call

### Provider deactivation (6 new tests — D7)
- `test_401_deactivates_provider_for_60s` — 401 inserts reactivation time = now + 60s
- `test_403_deactivates_provider_for_60s` — 403 inserts reactivation time = now + 60s
- `test_429_deactivates_provider_for_30s` — 429 inserts reactivation time = now + 30s
- `test_400_does_not_deactivate_provider` — 400 does NOT insert into cooldown map
- `test_404_does_not_deactivate_provider` — 404 does NOT insert into cooldown map
- `test_cooldown_active_routing_skips_provider` — provider with future reactivation is skipped
- `test_expired_cooldown_allows_routing` — provider with past reactivation time routes normally

### Implementation verification (code review check)
- Verify DashMap cooldown check appears in ALL THREE fallback Err(e) branches:
  - server/mod.rs ~946 (standard requests)
  - server/mod.rs ~1198 (streaming requests)
  - server/mod.rs ~1518 (OpenAI-compatible requests)
- Run: `grep -n "provider_cooldowns" src/server/mod.rs` — should show 3+ hits in Err branches

## Browser DevTools Tests (admin.html — no JS framework)

### escapeJs injection fix (item 11)
- Test: add an OAuth provider with name containing `'` (e.g. `test'provider`) via admin UI
- Expected: refreshOAuthToken and deleteOAuthToken buttons still work (no JS syntax error)
- Test: add an OAuth provider with name containing `\` — buttons still work
- Verify: `escapeJs` function is present and handles `\`, `'`, `"` correctly

### loadConfig 401 cancel (item 12)
- Test: with server requiring API key, open admin UI, cancel the API key prompt
- Expected: clear error message shown (e.g. "API key required"), no unhandled promise rejection in console
- Verify: console shows no `SyntaxError: Unexpected token` or `JSON.parse` errors

## Critical Paths (end-to-end)
- Copilot OAuth provider background refresh still works after generalization
- Provider deactivated by 429 is skipped in fallback chain for all request types (standard + streaming + OpenAI-compat)
- Provider auto-reactivates after cooldown without server restart
