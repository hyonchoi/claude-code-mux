# Test Plan
Generated by /plan-eng-review on 2026-05-05
Branch: main
Repo: 9j/claude-code-mux

## Affected Pages/Routes
- /v1/messages proxy path through OpenAI-compatible provider routing — validate nvidia-nim dispatch and endpoint assembly.
- Admin provider configuration flow in server UI — validate nvidia-nim config can be added/saved and is loaded at startup.

## Key Interactions to Verify
- Configure provider_type nvidia-nim with api key from environment, start server, and execute non-codex message request.
- Execute codex-like model request path to confirm /responses endpoint branch is still correct after URL normalization changes.
- Execute streaming request and verify SSE response behavior does not regress.
- Trigger upstream non-2xx response and verify mapped error returned to client is clear and non-silent.

## Edge Cases
- base_url with /v1 versus without /v1 for nvidia-nim.
- Missing env variable for api_key should fail with actionable startup error.
- Unsupported env syntax ${VAR} should produce explicit expected failure behavior.
- Existing openai-compatible providers (openrouter/groq/deepinfra) remain unaffected by normalization logic.

## Critical Paths
- Config parse -> provider registry construction -> endpoint selection -> upstream request -> mapped response/error.
- Shared OpenAIProvider endpoint assembly path across existing providers and new nvidia-nim provider.
